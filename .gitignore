# Env — arquivos com segredos (não comitar)
.env
.env.local
frontend/.env
backend/.env
backend/.env.local

# Env — .env.development e .env.production SÃO comitados (sem segredos, apenas URLs públicas)

# Dependencies & build output
node_modules
frontend/node_modules
frontend/dist
backend/dist

# SQLite local (banco de dados de desenvolvimento)
backend/local.db
backend/local.db-shm
backend/local.db-wal
backend/.pz-rank-local.db
backend/*.db

# PZ mod (pasta separada, não faz parte do repositório web)
mod

# Companion (repositório separado — PZ-Rank-Companion)
companion/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Updates
/frontend/updates
